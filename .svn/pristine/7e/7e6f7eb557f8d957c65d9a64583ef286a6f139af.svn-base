package com.flyrish.hades.util;

import javax.servlet.http.HttpSessionAttributeListener;
import javax.servlet.http.HttpSessionBindingEvent;

public class AttributeListenTest implements HttpSessionAttributeListener {

	@Override
	public void attributeAdded(HttpSessionBindingEvent htsession) {
		if(htsession.getName().equals("campus")){
		
		}
	}

	@Override
	public void attributeRemoved(HttpSessionBindingEvent htsession) {
		if(htsession.getName().equals("campus")){
			
		}
	}

	@Override
	public void attributeReplaced(HttpSessionBindingEvent htsession) {
		if(htsession.getName().equals("campus")){
		
		}
	}

}
